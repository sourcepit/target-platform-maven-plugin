# environment variables
env.JAVA_HOME=${java.home}
env.M2_HOME=${it.bin.dir}/apache-maven-${it.maven.version}
env.MAVEN_OPTS=${test.memArgs} -Duser.home=${it.user.home}
env.PATH=${java.home}/bin${path.separator}${env.PATH}

# system properties
project.version=${project.version}
it.resources=${project.build.testOutputDirectory}
it.localRepository=${it.localRepository}
resources.dir=${project.build.testOutputDirectory}
build.dir=${it.dir}
user.home=${it.user.home}
java.home=${java.home}
debug.allowed=${it.debugAllowed}
http.proxyHost=${http.proxyHost}
http.proxyPort=${http.proxyPort}
http.nonProxyHosts=${http.nonProxyHosts}
